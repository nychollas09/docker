const path = require('path');

module.exports = {
  config: path.resolve(
    path.join(__dirname, "src", "app", "config"),
    "config.js"
  ),
  "models-path": path.resolve(
    path.join(__dirname, "src", "app", "domain"),
    "models"
  ),
  "migrations-path": path.resolve(
    path.join(__dirname, "src", "app", "domain"),
    "migrations"
  ),
  "seeders-path": path.resolve(
    path.join(__dirname, "src", "app", "domain"),
    "seeds"
  ),
};